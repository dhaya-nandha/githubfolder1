#include <iostream>
#include <cmath>

int main() {
    std:: cout << "Vanakamma da mapla !\n";
    
    //basic math funcions
    double x = 2.3;
    double y = 5.9;
    double r;

    //r = std::max(x, y);
    //r = std::min(x, y);
    //r = std::pow(2, 4);
    //r = std:: sqrt(25);
    //r = std::round(x);
    //r = std::ceil(y);
    //r = std::floor(y);
    //r = std::abs(-55.55);

    std::cout << r;

    return 0;
}